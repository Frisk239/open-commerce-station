# Config

Composes one deploy into exactly one Station flavor. It selects default language, accounting currency, filing fields and payment capabilities without owning commerce behavior or provider transports.
