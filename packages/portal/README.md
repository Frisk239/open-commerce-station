# Merchant Portal module

This module owns the complete Store identity edit operation: form validation, disk-image staging, PostgreSQL persistence, replacement cleanup, and the shared form. Station applications provide authentication and a flavor constant, then call one deep interface.
